import type { NorthStar } from './types.js';

export const northStar: NorthStar = {
  title: 'One canonical DJ library, plus a realistic model of how it behaves in the booth.',
  summary:
    'DJ Vault should let artists ingest, normalize, analyze, export, inspect, and emulate their music workflows without depending on cloud lock-in or vendor-owned truth.',
};
