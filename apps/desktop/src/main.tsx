import React from 'react';
import ReactDOM from 'react-dom/client';
import { VaultBuddyApp } from './VaultBuddyApp';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <VaultBuddyApp />
  </React.StrictMode>,
);
